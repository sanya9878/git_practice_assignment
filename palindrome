

function detectPalindrome(num){
let bag="";
let str="";
str+=num;
for(let i=str.length-1;i>=0;i--){
 bag+=str[i];   
}
  (bag == num ? console.log("Yes"):console.log("No"));
}
detectPalindrome(1221)
